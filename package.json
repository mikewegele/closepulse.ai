{
  "name": "closepulse.ai",
  "version": "1.0.0",
  "private": true,
  "author": "closepulse.ai Team",
  "license": "MIT",
  "scripts": {
    "dev": "npm run frontend",
    "backend": "docker compose up -d api",
    "docker": "docker compose build --no-cache api\ndocker compose up -d api",
    "frontend": "cd pip_frontend && npm run dev",
    "frontend:build": "cd pip_frontend && npm run build",
    "frontend:preview": "cd pip_frontend && npm run preview",
    "backend:docker": "docker compose up api",
    "tunnel": "cloudflared tunnel run closepulse",
    "tunnel:docker": "docker run -it --rm --network host cloudflare/cloudflared:latest \\\n  tunnel --url http://localhost:8000\n",
    "backend:docker:stop": "docker compose down",
    "app": "concurrently -n fe,tunnel -c yellow,blue \"npm run frontend\" \"npm run tunnel\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
